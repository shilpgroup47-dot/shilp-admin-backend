name: Build and Deploy Check

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'docs/**'

jobs:
  build-and-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🚚 Checkout code
      uses: actions/checkout@v3

    - name: 📂 Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm ci

    - name: ✅ Build check
      run: npm run build

    - name: 🚀 Deployment Ready
      run: |
        echo "✅ Dependencies installed successfully"
        echo "✅ Build process completed"
        echo "📋 Project ready for deployment to cPanel"
        echo ""
        echo "� Manual deployment steps:"
        echo "1. Upload files to /home/shilfmfe/server_running/backend.shilpgroup.com/"
        echo "2. Set permissions: chmod +x *.sh"
        echo "3. Run deployment: ./auto-deploy.sh"
        echo ""
        echo "📖 See DEPLOYMENT-GUIDE.md for detailed instructions"